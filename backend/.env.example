# ============================================
# Agent Commerce Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# ============================================
# Server Configuration
# ============================================
# Port for the FastAPI server (default: 8000)
PORT=8000

# ============================================
# Supabase Configuration (Required)
# ============================================
# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase service role key (has admin privileges, bypasses RLS)
# ⚠️ Keep this secret! Never commit to version control
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ============================================
# Wallet & Encryption Configuration
# ============================================
# Secret key for encrypting agent private keys
# ⚠️ Keep this secret! Used to encrypt private keys before storing in database
# Generate a strong random string (e.g., 32+ characters)
USER_SECRET_KEY=your_encryption_secret_key_here

# ============================================
# Admin Wallet Configuration (Required for Auto-Funding)
# ============================================
# Admin wallet private key - used to fund new agent wallets
# ⚠️ Keep this secret! This wallet should have ETH on Base Sepolia
ADMIN_PRIVATE_KEY=your_admin_wallet_private_key_here

# Admin wallet public address (optional, for reference/logging)
ADMIN_PUBLIC_KEY=your_admin_wallet_address_here

# Amount of ETH to send to each newly created agent wallet (in ETH, not Wei)
# This amount should be sufficient for agent registration and initial operations
# Example: 0.001 means 0.001 ETH will be sent to each agent wallet
CREATION_GAS_FEE=0.001

# ============================================
# Base Network Configuration
# ============================================
# Base Sepolia RPC URL (optional, defaults to public RPC)
# You can use a custom RPC provider for better reliability
BASE_RPC_URL=https://sepolia.base.org

# ============================================
# ChaosChain Configuration
# ============================================
# ChaosChain network (optional, defaults to BASE_SEPOLIA)
# Options: BASE_SEPOLIA, BASE_MAINNET, etc.
CHAOSCHAIN_NETWORK=BASE_SEPOLIA

# ============================================
# Notes
# ============================================
# 1. All private keys and secrets should be kept secure
# 2. Never commit .env file to version control
# 3. For production, use environment variables or secret management services
# 4. Make sure admin wallet has sufficient ETH for funding agent wallets
# 5. Each agent uses its own wallet for all transactions and fees
# 6. Base Sepolia testnet ETH can be obtained from faucets:
#    - https://www.coinbase.com/faucets/base-ethereum-sepolia-faucet
#    - https://www.alchemy.com/faucets/base-sepolia

# Alternative RPC endpoints (if default doesn't work):
# - https://base-sepolia.gateway.tatum.io (FreeRPC)
# - https://base-testnet.rpc.grove.city/v1/01fdb492 (Grove)
# - https://base-sepolia-rpc.publicnode.com (PublicNode)
# - For production, use Alchemy or Infura with API key:
#   https://base-sepolia.g.alchemy.com/v2/YOUR-API-KEY
#   https://base-sepolia.infura.io/v3/YOUR-API-KEY
